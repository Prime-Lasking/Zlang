; Print first 10 Fibonacci numbers
MOV a, 0
MOV b, 1
MOV count, 10
MOV i, 0

fib_loop:
    PRINT a
    MOV temp, a
    ADD temp, b
    MOV a, b
    MOV b, temp
    
    ADD i, 1
    CMP i, count
    JNZ fib_loop

HALT
